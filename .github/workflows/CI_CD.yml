name: Combined CI / Release

on:
  workflow_dispatch:
  push:
    tags:
      - '*'
    branches:
      - '**'

jobs:
  ci_cd:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: CI/CD
        id: mu88-ci-cd
        uses: mu88/github-actions/ci-cd@0.1
        with:
          github-ref: ${{ github.ref }}
          sonar-key: 'mu88_RaspiFanController'
          sonar-token: ${{ secrets.SONAR_TOKEN }}
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          registry: registry.hub.docker.com
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build and push Docker non-release
        if: ${{ steps.mu88-ci-cd.outputs.is-release != 'true' }}
        run: |
          dotnet publish RaspiFanController/RaspiFanController.csproj /t:MultiArchPublish '-p:ContainerImageTags="${{ steps.mu88-ci-cd.outputs.version }}"' -p:ContainerRegistry=registry.hub.docker.com
          dotnet publish RaspiFanController/RaspiFanController.csproj /t:MultiArchPublish '-p:ContainerImageTags="${{ steps.mu88-ci-cd.outputs.version }}-chiseled"' -p:ContainerFamily=jammy-chiseled -p:ContainerRegistry=registry.hub.docker.com
      - name: Build and push Docker release
        if: ${{ steps.mu88-ci-cd.outputs.is-release == 'true' }}
        run: |
          dotnet publish RaspiFanController/RaspiFanController.csproj /t:MultiArchPublish '-p:ContainerImageTags="${{ steps.mu88-ci-cd.outputs.version }};latest"' -p:ContainerRegistry=registry.hub.docker.com
          dotnet publish RaspiFanController/RaspiFanController.csproj /t:MultiArchPublish '-p:ContainerImageTags="${{ steps.mu88-ci-cd.outputs.version }}-chiseled;latest-chiseled"' -p:ContainerRegistry=registry.hub.docker.com -p:ContainerFamily=jammy-chiseled
      - name: .NET Publish
        run: dotnet publish RaspiFanController/RaspiFanController.csproj -r linux-arm64 /p:PublishSingleFile=true --self-contained
      - name: Create artifact
        if: ${{ steps.mu88-ci-cd.outputs.is-release == 'true' }}
        run: |
          cd /home/runner/work/RaspiFanController/RaspiFanController/RaspiFanController/bin/Release/net9.0/linux-arm64/publish
          zip -r RaspiFanController.zip *
      - name: Create GitHub Release
        id: create_release
        uses: actions/create-release@v1
        if: ${{ steps.mu88-ci-cd.outputs.is-release == 'true' }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ steps.mu88-ci-cd.outputs.version }}
          draft: false
          prerelease: false
      - name: Upload asset to GitHub Release
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        if: ${{ steps.mu88-ci-cd.outputs.is-release == 'true' }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }} # This pulls from the CREATE RELEASE step above, referencing it's ID to get its outputs object, which include a `upload_url`. See this blog post for more info: https://jasonet.co/posts/new-features-of-github-actions/#passing-data-to-future-steps 
          asset_path: /home/runner/work/RaspiFanController/RaspiFanController/RaspiFanController/bin/Release/net9.0/linux-arm64/publish/RaspiFanController.zip
          asset_name: RaspiFanController.zip
          asset_content_type: application/zip
